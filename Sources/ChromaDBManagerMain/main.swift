import ChromaDBManagerApp

// Точка входа и больше ничего.
//
// Само приложение — библиотека `ChromaDBManagerApp`, а не исполняемая цель,
// и это не украшение сборки: тестовый набор **не может** импортировать
// исполняемую цель, поэтому весь слой экранов — 9700 строк моделей и 15 600
// строк вьюх — не покрывался ничем. Дефекты …,, жили
// именно там и проверялись чтением.
//
// Здесь остаётся ровно одна строка: всё остальное живёт в библиотеке, куда
// тесты дотягиваются через `@testable import`.
ChromaDBManagerLauncher.run()
